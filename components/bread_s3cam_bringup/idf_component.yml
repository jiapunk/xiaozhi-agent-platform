dependencies:
  espressif/esp_audio_codec: ~2.5.0
  espressif/esp_websocket_client: ~1.8.0
  # Reproduce the complete camera stack available when the device's proven
  # XiaoZhi 2.1.0 image was built (2025-11-11).  esp_video's transitive
  # ranges otherwise select newer SCCB/sensor releases with changed bus
  # transaction behavior.
  espressif/esp_video: ==1.3.1
  espressif/esp_cam_sensor: ==1.5.1
  espressif/esp_sccb_intf: ==0.0.5
  idf:
    version: ">=5.5.1,<6.0.0"
